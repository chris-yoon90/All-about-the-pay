<% provide(:title, full_title("Edit #{@employee.name}")) %>
<!-- <div class="row">
	<div class="col-xs-6 col-xs-offset-3">
		<h2>Information of <%= @employee.name %></h2>
		<%= form_for(@employee) do |f|%>
			<%= render 'shared/error_messages_template', model: @employee %>
			<%= render 'shared/employee_form', f: f, password: "" %>
		<% end %>
	</div>
</div> -->