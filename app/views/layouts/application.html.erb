<!DOCTYPE html>
<html>
<head>
  <title>AllAboutThePay</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	<%= render 'layouts/header' %>
	<div class="container">
		
		<%= yield %>

		<% if Rails.env.development? %>
			<!--<%= debug("Original request: #{request.method} Overridden request: #{request.request_method} Original URL: #{request.original_url}") %> -->
			<%= debug(params) %>
		<% end %>
	</div>

</body>
</html>
