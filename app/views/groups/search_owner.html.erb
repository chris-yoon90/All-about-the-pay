<% provide(:title, "Choose an owner for #{@group.name}") %>

<%= render layout: 'search_layout', locals: { employees: @employees } do %>
	<%= render partial: 'ownership_form', collection: @employees, as: :employee, locals: { group: @group }, layout: 'employee_item_list_layout' %>
<% end %>
